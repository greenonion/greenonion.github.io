<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Worn-out Shoes</title>
    <link>https://greenonion.github.io/index.xml</link>
    <description>Recent content on Worn-out Shoes</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en-us</language>
    <copyright>Copyright © 2017 Nikos Fertakis</copyright>
    <lastBuildDate>Fri, 16 Jun 2017 17:23:35 +0300</lastBuildDate>
    <atom:link href="https://greenonion.github.io/index.xml" rel="self" type="application/rss+xml" />
    
    <item>
      <title>SICP Solutions: Sections 1.2.3 and 1.2.4</title>
      <link>https://greenonion.github.io/blog/sicp-solutions-sections-1.2.3-and-1.2.4/</link>
      <pubDate>Fri, 16 Jun 2017 17:23:35 +0300</pubDate>
      
      <guid>https://greenonion.github.io/blog/sicp-solutions-sections-1.2.3-and-1.2.4/</guid>
      <description>

&lt;h3 id=&#34;section-1-2-3-orders-of-growth&#34;&gt;Section 1.2.3 - Orders of Growth&lt;/h3&gt;

&lt;h4 id=&#34;exercise-1-14&#34;&gt;Exercise 1.14&lt;/h4&gt;

&lt;p&gt;The tree is big :)&lt;/p&gt;

&lt;p&gt;The recursive process reaches maximum depth when the denomination is a penny,
for example &lt;code&gt;(cc 11 1)&lt;/code&gt; will be $11$. This means that the growth of space will
be $\Theta(n)$, where $n$ is the amount we want to change.&lt;/p&gt;

&lt;p&gt;For each denomination sub-tree, the maximum steps we will need to calculate is
where the only remaining denomination is the penny, and the steps required will
be exactly $n$. Since we have $5$ denominations, the time complexity will be
$O(n^5)$.&lt;/p&gt;

&lt;h4 id=&#34;exercise-1-15&#34;&gt;Exercise 1.15&lt;/h4&gt;

&lt;h5 id=&#34;a&#34;&gt;a.&lt;/h5&gt;

&lt;p&gt;The procedure &lt;code&gt;p&lt;/code&gt; will be applied each time &lt;code&gt;angle&lt;/code&gt; is greater than $0.1$.
Between iterations, &lt;code&gt;angle&lt;/code&gt; will be divided by $3$. This means that with a
starting &lt;code&gt;angle&lt;/code&gt; of $12.15$ we will have &lt;code&gt;p&lt;/code&gt; applied for $12.15, 4.16, 1.38,
0.46$ and finally for $0.15$. So &lt;code&gt;p&lt;/code&gt; will be applied $5$ times.&lt;/p&gt;

&lt;h5 id=&#34;b&#34;&gt;b.&lt;/h5&gt;

&lt;p&gt;The space required will be growing on the order of $O(\log_3(n))$, because each
step will require to remember its parent to return to. Similarly, the time
required will be $O(\log_3(n))$ as well.&lt;/p&gt;

&lt;h3 id=&#34;section-1-2-4-exponentiation&#34;&gt;Section 1.2.4 - Exponentiation&lt;/h3&gt;

&lt;h4 id=&#34;exercise-1-16&#34;&gt;Exercise 1.16&lt;/h4&gt;

&lt;p&gt;Using the definition of &lt;code&gt;even?&lt;/code&gt; from before:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-klipse-eval-scheme&#34;&gt;(define (fast-expr b n)
  (fast-expr-iter b n 1))

(define (fast-expr-iter b n a)
  (cond ((= n 1) a)
        ((even? n) (fast-expr-iter b
                                   (/ n 2)
                                   (* a (* b b))))
        (else (fast-expr-iter b (- n 1) (* a b)))))

(fast-expr 2 3)
&lt;/code&gt;&lt;/pre&gt;

&lt;h4 id=&#34;exercise-1-17&#34;&gt;Exercise 1.17&lt;/h4&gt;

&lt;p&gt;A recursive process:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-klipse-eval-scheme&#34;&gt;(define (double a) (* 2 a))

(define (halve a) (/ a 2))

(define (fast-multi a b)
  (cond ((= a 1) b)
        ((even? a) (fast-multi (halve a) (double b)))
        (else (+ b (fast-multi (- a 1) b)))))

(fast-multi 5 8)
&lt;/code&gt;&lt;/pre&gt;

&lt;h4 id=&#34;exercise-1-18&#34;&gt;Exercise 1.18&lt;/h4&gt;

&lt;p&gt;Similarly to exercise 1.16, we will use a variable as accumulator:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-klipse-eval-scheme&#34;&gt;(define (double a) (* 2 a))

(define (halve a) (/ a 2))

(define (fast-mult a b)
  (fast-mult-iter a b 0))

(define (fast-mult-iter a b n)
  (cond ((= a 0) n)
        ((even? a) (fast-mult-iter (halve a) (double b) n))
        (else (fast-mult-iter (- a 1) b (+ b n)))))

(fast-multi 5 8)
&lt;/code&gt;&lt;/pre&gt;

&lt;h4 id=&#34;exercise-1-19&#34;&gt;Exercise 1.19&lt;/h4&gt;

&lt;p&gt;First, we need to apply $T$ twice, to compute $T^2$. We start with:&lt;/p&gt;

&lt;div&gt;$$

\begin{align*}
  a &amp;\leftarrow bq + aq + ap\\
  b &amp;\leftarrow bp + aq
\end{align*}

$$&lt;/div&gt;

&lt;p&gt;Which will then become:&lt;/p&gt;

&lt;div&gt;$$

\begin{align*}
  a &amp;\leftarrow (bp + aq)q + (bp + aq + ap)q + (bq + aq + ap)p\\
  b &amp;\leftarrow (bp + aq)p + (bp + aq + ap)q
\end{align*}

$$&lt;/div&gt;

&lt;p&gt;Let&amp;rsquo;s expand the first one:&lt;/p&gt;

&lt;div&gt;$$

\begin{align*}
  a &amp;\leftarrow (bp + aq)q + (bp + aq + ap)q + (bq + aq + ap)p\\
    &amp;= bpq + aq^2 + bq^2 + aq^2 + apq + bpq + apq + ap^2 \\
    &amp;= b(2pq + q^2) + a(2pq + q^2) + a(q^2 + p^2)
\end{align*}

$$&lt;/div&gt;

&lt;p&gt;And in the same manner the second one:&lt;/p&gt;

&lt;div&gt;$$

\begin{align*}
  b &amp;\leftarrow (bp + aq)p + (bp + aq + ap)q\\
    &amp;= bp^2 + apq + bq^2 + aq^2 + apq\\
    &amp;= b(p^2 + q^2) + a(2pq + q^2)
\end{align*}

$$&lt;/div&gt;

&lt;p&gt;So it turns out that we can name variables $p&amp;rsquo;$ and $q&amp;rsquo;$:&lt;/p&gt;

&lt;div&gt;$$

\begin{align*}
  p&#39; &amp;= p^2 + q^2\\
  q&#39; &amp;= 2pq + q^2
\end{align*}

$$&lt;/div&gt;

&lt;p&gt;These values will essentially square $T$. So the logarithimic version of the
Fibonacci procedure can be written as:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-klipse-eval-scheme&#34;&gt;(define (square n) (* n n))

(define (fib n)
  (fib-iter 1 0 0 1 n))

(define (fib-iter a b p q count)
  (cond ((= count 0) b)
        ((even? count)
         (fib-iter a
                   b
                   (+ (square p) (square q))
                   (+ (* 2 p q) (square q))
                   (/ count 2)))
        (else (fib-iter (+ (* b q) (* a q) (* a p))
                        (+ (* b p) (* a q))
                        p
                        q
                        (- count 1)))))

(fib 9)
&lt;/code&gt;&lt;/pre&gt;
</description>
    </item>
    
    <item>
      <title>SICP Solutions: Sections 1.2.5 and 1.2.6</title>
      <link>https://greenonion.github.io/blog/sicp-solutions-sections-1.2.5-and-1.2.6/</link>
      <pubDate>Fri, 16 Jun 2017 17:23:35 +0300</pubDate>
      
      <guid>https://greenonion.github.io/blog/sicp-solutions-sections-1.2.5-and-1.2.6/</guid>
      <description>

&lt;h3 id=&#34;section-1-2-5-greatest-common-divisors&#34;&gt;Section 1.2.5 - Greatest Common Divisors&lt;/h3&gt;

&lt;h4 id=&#34;exercise-1-20&#34;&gt;Exercise 1.20&lt;/h4&gt;

&lt;p&gt;The difference between applicative and normal order is that applicative first
evaluates the arguments and then applies the procedure. In contrast, normal
order first expands the procedure and then reduces it. Also note that &lt;code&gt;if&lt;/code&gt; is a
special form, where which (and whether) procedure is evaluated depends on the
predicate.&lt;/p&gt;

&lt;p&gt;Let&amp;rsquo;s first see the applicative order:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-scheme&#34;&gt;(gcd 206 40) ;; apply if, second branch produces
(gcd 40 (remainder 206 40)) ;; remember, we evaluate arguments first
(gcd 40 6)
(gcd 40 (remainder 40 6)) ;; 2nd time we need remainder
(gcd 6 4)
(gcd 4 (remainder 4 6)) ;; 3rd time
(gcd 4 2)
(gcd 2 (remainder 4 2)) ;; 4th time
(gcd 2 0) ;; the predicate is true
2
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;In the normal order, we first expand and then apply only as needed. So:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-scheme&#34;&gt;(gcd 206 40) ;; apply if, second branch produces
(gcd 40 (remainder 206 40)) ;; the if will be as follows
;; apply gcd
(if (= (remainder 206 40) 0) ;; this gets evaluated, so 1 call
    40
    (gcd (remainder 206 40)
         (remainder 40 (remainder 206 40))))
;; apply gcd
(if (= (remainder 40 (remainder 206 40)) 0) ;; 2 calls
    (remainder 206 40)
    (gcd (remainder 40 (remainder 206 40))
         (remainder
           (remainder 206 40
             (remainder 40 (remainder 206 40))))))
;; apply gcd
(if (= (rem (rem 206 40) (rem 40 (rem 206 40))) 0) ;; 4 calls
    (rem 40 (rem 206 40))
    (gcd (rem (rem 206 40) (rem 40 (rem 206 40)))
         (rem
           (rem 40 (rem 206 40))
           (rem (rem 206 40) (rem 40 (rem 206 40))))))
;; apply gcd
;; this time the predicate will be b from the previous
;; iteration which contains 7 calls to remainder but
;; evaluates to 0 so we evaluate a from the previous
;; iteration (4 calls to remainder) and we are done.
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;So in summary, the normal order will need:
$1 + 2 + 4 + 7 + 4 = 18$ calls to &lt;code&gt;remainder&lt;/code&gt;.&lt;/p&gt;

&lt;h3 id=&#34;section-1-2-6-example-testing-for-primality&#34;&gt;Section 1.2.6 - Example: Testing for Primality&lt;/h3&gt;

&lt;h4 id=&#34;exercise-1-21&#34;&gt;Exercise 1.21&lt;/h4&gt;

&lt;pre&gt;&lt;code class=&#34;language-scheme&#34;&gt;&amp;gt; (smallest-divisor 199)
199
&amp;gt; (smallest-divisor 1999)
1999
&amp;gt; (smallest-divisor 19999)
7
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;So while $199$ and $1999$ are primes, $19999$ is not.&lt;/p&gt;

&lt;h4 id=&#34;exercise-1-22&#34;&gt;Exercise 1.22&lt;/h4&gt;

&lt;p&gt;$\sqrt{10} = 3.17$. This means that we expect time to roughly triple between
orders of magnitude. Our results are:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-scheme&#34;&gt;;; Greater than 1,000
;; 1009 *** 3
;; 1013 *** 2
;; 1019 *** 2
;; Greater than 10,000
;; 10007 *** 6
;; 10009 *** 6
;; 10037 *** 6
;; Greater than 100,000
;; 100003 *** 17
;; 100019 *** 18
;; 100043 *** 18
;; Greater than 1,000,000
;; 1000003 *** 59
;; 1000033 *** 59
;; 1000037 *** 59
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The results roughly confirm our calculations. We wouldn&amp;rsquo;t expect exact results
since the actual timing depends on a lot of things.&lt;/p&gt;

&lt;h4 id=&#34;exercise-1-23&#34;&gt;Exercise 1.23&lt;/h4&gt;

&lt;pre&gt;&lt;code class=&#34;language-scheme&#34;&gt;(define (next n)
  (if (= n 2)
      3
      (+ n 2)))
&lt;/code&gt;&lt;/pre&gt;

&lt;pre&gt;&lt;code&gt;;; 1009 *** 6
;; 1013 *** 6
;; 1019 *** 6
;; 10007 *** 9
;; 10009 *** 8
;; 10037 *** 8
;; 100003 *** 16
;; 100019 *** 15
;; 100043 *** 16
;; 1000003 *** 41
;; 1000033 *** 40
;; 1000037 *** 40
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Essentially the steps taken by the algorithm will now be limited by
$\sqrt{n}/2$. This means that $\sqrt{n}$ will still dominate the algorithm, but
given a large-enough input the time will be roughly halved.&lt;/p&gt;

&lt;h4 id=&#34;exercise-1-24&#34;&gt;Exercise 1.24&lt;/h4&gt;

&lt;pre&gt;&lt;code class=&#34;language-scheme&#34;&gt;;; 1009 *** 13
;; 1013 *** 13
;; 1019 *** 13
;; 10007 *** 15
;; 10009 *** 14
;; 10037 *** 14
;; 100003 *** 21
;; 100019 *** 16
;; 100043 *** 16
;; 1000003 *** 17
;; 1000033 *** 18
;; 1000037 *** 19
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;We would expect the time to test primes near $1,000,000$ to be roughly the
triple of the time to test primes near $1,000$. We do not obseve even that kind
of change, probably due to fast hardware, but we do observe that the runtime
grows very slowly.&lt;/p&gt;

&lt;h4 id=&#34;exercise-1-25&#34;&gt;Exercise 1.25&lt;/h4&gt;

&lt;p&gt;While conceptually Alyssa is correct, in practice the call to &lt;code&gt;fast-expt&lt;/code&gt; will
result in dealing with big numbers, and trying to find their &lt;code&gt;remainder&lt;/code&gt;. Our
&lt;code&gt;expmod&lt;/code&gt; method will always find the remainder of small numbers, multiply them
together, find their remainder, etc. See footnote 46. Bear in mind also that
primality checks are usually done for very large numbers.&lt;/p&gt;

&lt;p&gt;The results are pretty grave in practice:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-scheme&#34;&gt;;; 1009 *** 92
;; 1013 *** 102
;; 1019 *** 83
;; 10007 *** 3071
;; 10009 *** 3192
;; 10037 *** 2880
&lt;/code&gt;&lt;/pre&gt;

&lt;h4 id=&#34;exercise-1-26&#34;&gt;Exercise 1.26&lt;/h4&gt;

&lt;p&gt;By rewriting the &lt;code&gt;expmod&lt;/code&gt; procedure to use explicit multiplication, Louis has
changed it into a tree recursive process from a linear one. This means that in
each steps two subtrees will be created, and the amount of calls grow
exponentially. So $2^{\log n} = n$.&lt;/p&gt;

&lt;h4 id=&#34;exercise-1-27&#34;&gt;Exercise 1.27&lt;/h4&gt;

&lt;p&gt;We create a procedure &lt;code&gt;congruent?&lt;/code&gt; as such:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-scheme&#34;&gt;(define (congruent? n)
  (congruent-iter n 0))

(define (congruent-iter n a)
  (cond ((= a n) true)
        ((= (expmod a n n) a) (congruent-iter n (+ a 1)))
        (else false)))
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Calling the procedure for the Carmichael numbers of footnote 47 (561, 1105,
1729, 2465, 2821 and 6601) we always get &lt;code&gt;#t&lt;/code&gt;. The &lt;code&gt;fast-prime?&lt;/code&gt; procedure would
be fooled. However, we can use the &lt;code&gt;prime?&lt;/code&gt; procedure to confirm that these
numbers are not primes.&lt;/p&gt;

&lt;h4 id=&#34;exercise-1-28&#34;&gt;Exercise 1.28&lt;/h4&gt;

&lt;p&gt;First we create the function that performs the check and short-circuits the
computation:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-scheme&#34;&gt;(define (miller-rabin-check a m)
  (if (and (not (= a 1))
           (not (= a (- m 1)))
           (= (remainder (square a) m) 1))
      0
      a))
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;We want to use the above before squaring the result of each iteration. Since we
cannot store variables yet, what we can do is wrap the &lt;code&gt;expmod&lt;/code&gt; call to itself
inside a &lt;code&gt;miller-rabin-check&lt;/code&gt; call. &lt;code&gt;miller-rabin-check&lt;/code&gt; will get the result of
the previous iteration as input, and check whether it is a non-trivial square
root of 1. If it is, it will return 0, essentially short-circuting the result.
If it is not, it will return the result, thus letting &lt;code&gt;expmod&lt;/code&gt; continue as
usual.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-scheme&#34;&gt;(define (miller-rabin-expmod base exp m)
  (cond ((= exp 0) 1)
        ((even? exp)
         (remainder
          (square
           (miller-rabin-check
            (miller-rabin-expmod base (/ exp 2) m) m)) m))
        (else
         (remainder (* base
          (miller-rabin-expmod base (- exp 1) m))
          m))))
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Finally, we can run the check for half the size of the input to make sure we
produce the correct answer:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-scheme&#34;&gt;(define (miller-rabin-test n)
  (define (try-it a)
    (= (miller-rabin-expmod a (- n 1) n) 1))
  (try-it (+ 1 (random (- n 1)))))

(define (miller-rabin-prime? n)
  (define (test-prime times)
    (cond ((&amp;lt;= times 0) true)
          ((miller-rabin-test n) (test-prime (- times 1)))
          (else false)))
  (test-prime (/ n 2)))
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;We can use both the prime numbers we found and the Carmichael numbers to confirm
that our new procedure always produces the correct result and is not fooled.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>SICP Solutions: Sections 1.2.1 and 1.2.2</title>
      <link>https://greenonion.github.io/blog/sicp-solutions-sections-1.2.1-and-1.2.2/</link>
      <pubDate>Fri, 16 Jun 2017 13:53:44 +0300</pubDate>
      
      <guid>https://greenonion.github.io/blog/sicp-solutions-sections-1.2.1-and-1.2.2/</guid>
      <description>

&lt;h2 id=&#34;section-1-2&#34;&gt;Section 1.2&lt;/h2&gt;

&lt;h3 id=&#34;section-1-2-1&#34;&gt;Section 1.2.1&lt;/h3&gt;

&lt;h4 id=&#34;exercise-1-9&#34;&gt;Exercise 1.9&lt;/h4&gt;

&lt;p&gt;First procedure:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-scheme&#34;&gt;(+ 4 5)
(inc (+ 3 5))
(inc (inc (+ 2 5)))
(inc (inc (inc (+ 1 5))))
(inc (inc (inc (inc (+ 0 5)))))
(inc (inc (inc (inc 5))))
(inc (inc (inc 6)))
(inc (inc 7))
(inc 8)
9
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Second procedure:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-scheme&#34;&gt;(+ 4 5)
(+ 3 6)
(+ 2 7)
(+ 1 8
(+ 0 9)
9
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The first process is a linear recursive process.
The second process is a linear iterative process.&lt;/p&gt;

&lt;p&gt;Hint: try to see whether calling itself is &lt;em&gt;absolutely&lt;/em&gt; the last thing a
procedure does.&lt;/p&gt;

&lt;h4 id=&#34;exercise-1-10&#34;&gt;Exercise 1.10&lt;/h4&gt;

&lt;p&gt;Expression &lt;code&gt;(A 1 10)&lt;/code&gt;:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-scheme&#34;&gt;(A 1 10)
(A 0
   (A 1 9))
(A 0
   (A 0
      (A 1 8
(A 0
   (A 0
      (A 0
         (A 1 7))))
(A 0
   (A 0
      (A 0
         (A 0
            (A 1 6)))))
(A 0
   (A 0
      (A 0
         (A 0
            (A 0
               (A 1 5))))))
(A 0
   (A 0
      (A 0
         (A 0
            (A 0
               (A 0
                  (A 1 4)))))))
(A 0
   (A 0
      (A 0
         (A 0
            (A 0
               (A 0
                  (A 0
                     (A 1 3))))))))
(A 0
   (A 0
      (A 0
         (A 0
            (A 0
               (A 0
                  (A 0
                     (A 0
                        (A 1 2)))))))))
(A 0
   (A 0
      (A 0
         (A 0
            (A 0
               (A 0
                  (A 0
                     (A 0
                        (A 0
                           (A 1 1))))))))))
(A 0
   (A 0
      (A 0
         (A 0
            (A 0
               (A 0
                  (A 0
                     (A 0
                        (A 0 2)))))))))
(A 0
   (A 0
      (A 0
         (A 0
            (A 0
               (A 0
                  (A 0
                     (A 0 4))))))))
 (A 0
    (A 0
       (A 0
          (A 0
             (A 0
                (A 0
                   (A 0 8)))))))
 (A 0
    (A 0
       (A 0
          (A 0
             (A 0
                (A 0 16))))))
(A 0
   (A 0
      (A 0
         (A 0
            (A 0 32)))))
(A 0
   (A 0
      (A 0
         (A 0 64))))
(A 0
   (A 0
      (A 0 128)))
(A 0
   (A 0 256))
(A 0 512)
1024
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;So &lt;code&gt;(A 1 x)&lt;/code&gt; is \(2^x\).&lt;/p&gt;

&lt;p&gt;Expression &lt;code&gt;(A 2 4)&lt;/code&gt;|:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-scheme&#34;&gt;(A 2 4)
(A 1
   (A 2 3))
;; So this is 2^(A 2 3), let&#39;s see
(A 2 3)
(A 1
   (A 2 2))
;; So 2^(2^(A 2 2))
(A 2 2)
(A 1
   (A 2 1))
;; So 2^(2^(2^(A 2 1)))
(A 2 1)
2
;; So finally 2^(2^(2^2))
;; Or 2^(2^4)
;; Or 2^16
;; 65,636
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;So &lt;code&gt;(A 2 x)&lt;/code&gt; is 2 to the power of 2, to the power of 2 .. &lt;code&gt;x&lt;/code&gt; times.&lt;/p&gt;

&lt;p&gt;Expression &lt;code&gt;(A 3 3)&lt;/code&gt;:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-scheme&#34;&gt;(A 3 3)
(A 2
   (A 3 2))
;; We know what (A 2 x) is, 2 to the power of 2 x times.
;; Let&#39;s see where (A 3 2) takes us:
(A 3 2)
(A 2
   (A 3 1))
(A 2 2)
;; Which is 2^2
;; and the starting expression is (A 2 4)
;; Which is exactly the same as before.
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;So again &lt;code&gt;(A 3 3)&lt;/code&gt; equals \(2^{16} = 65,636\).&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;&lt;code&gt;(f n)&lt;/code&gt; computes \(2n\).&lt;/li&gt;
&lt;li&gt;&lt;code&gt;(g n)&lt;/code&gt; computes \(2^n\).&lt;/li&gt;
&lt;li&gt;&lt;code&gt;(h n)&lt;/code&gt; computes 2 to the power of 2, to the power of 2 .. &lt;code&gt;x&lt;/code&gt; times.&lt;/li&gt;
&lt;/ul&gt;

&lt;h3 id=&#34;section-1-2-2&#34;&gt;Section 1.2.2&lt;/h3&gt;

&lt;h4 id=&#34;exercise-1-11&#34;&gt;Exercise 1.11&lt;/h4&gt;

&lt;p&gt;Recursive process:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-klipse-eval-scheme&#34;&gt;(define (f n)
  (if (&amp;lt; n 3)
      n
      (+ (f (- n 1))
         (* 2 (f (- n 2)))
         (* 3 (f (- n 3))))))

(f 4)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Iterative process:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-klipse-eval-scheme&#34;&gt;(define (f n)
  (if (&amp;lt; n 3)
      n
      (f-iter 2 1 0 (- n 2))))

(define (f-iter a b c count)
  (if (= count 0)
      a
      (f-iter (+ a (* 2 b) (* 3 c)) a b (- count 1))))

(f 4)
&lt;/code&gt;&lt;/pre&gt;

&lt;h4 id=&#34;exercise-1-12&#34;&gt;Exercise 1.12&lt;/h4&gt;

&lt;p&gt;We define a procedure &lt;code&gt;pascal&lt;/code&gt; that accepts a row and a column as arguments:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-klipse-eval-scheme&#34;&gt;(define (pascal x y)
  (if (or (= y 0) (= x y))
      1
      (+ (pascal (- x 1) (- y 1))
         (pascal (- x 1) y))))

(pascal 3 2)
&lt;/code&gt;&lt;/pre&gt;

&lt;h4 id=&#34;exercise-1-13&#34;&gt;Exercise 1.13&lt;/h4&gt;

&lt;p&gt;First, the induction.&lt;/p&gt;

&lt;p&gt;The base step, for $n = 0$:&lt;/p&gt;

&lt;div&gt;$$

\mathrm{Fib}(0) = (\phi^0 - \psi^0) / \sqrt{5} = 0 \textrm{, it holds.}

$$&lt;/div&gt;

&lt;p&gt;Assume it is true for $n$, we will prove that it is true for $n + 1$:&lt;/p&gt;

&lt;div&gt;$$
\begin{align*}

\mathrm{Fib}(n+1) &amp;= \frac{\phi^{n+1} - \psi^{n+1}}{\sqrt{5}}\\
\frac{\phi^{n+1} - \psi^{n+1}}{\sqrt{5}} &amp;= \mathrm{Fib}(n) + \mathrm{Fib}(n - 1)\\
\frac{\phi^{n+1} - \psi^{n+1}}{\sqrt{5}} &amp;= \frac{\phi^n - \psi^n}{\sqrt{5}} +
\frac{\phi^{n-1} - \psi^{n-1}}{\sqrt{5}}\\
\phi^{n+1} - \psi^{n+1} &amp;= \phi^n - \psi^n + \phi^{n-1} - \psi^{n-1}\\
\phi^{n-1}(\phi^2 - \phi - 1) &amp;= \psi^{n-1}(\psi^2 - \psi - 1)

\end{align*}
$$&lt;/div&gt;

&lt;p&gt;By definition, $\phi^2 = \phi + 1$. Notice that also $\psi^2 = \psi + 1$. This
means that both sides of the equation are $0$, and the induction holds.&lt;/p&gt;

&lt;p&gt;We have proven that&lt;/p&gt;

&lt;div&gt;$$
\begin{equation*}

\mathrm{Fib}(n) = \frac{\phi^n - \psi^n}{\sqrt{5}}

\end{equation*}
$$&lt;/div&gt;

&lt;p&gt;Now we need to prove that $\vert\frac{\psi^n}{\sqrt{5}}\vert &amp;lt; \frac{1}{2}$.&lt;/p&gt;

&lt;p&gt;Notice that $\psi &amp;lt; \frac{1}{2}$, and hence $\vert\psi^{n+1}\vert &amp;lt; \vert\psi^n\vert, \forall n \in N$.
Since $\vert\frac{\psi^0}{\sqrt{5}}\vert &amp;lt; \frac{1}{2}$, it holds for all the rest values
of $n$.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>SICP Solutions: Section 1.1.7</title>
      <link>https://greenonion.github.io/blog/sicp-solutions-section-1.1.7/</link>
      <pubDate>Fri, 16 Jun 2017 13:10:57 +0300</pubDate>
      
      <guid>https://greenonion.github.io/blog/sicp-solutions-section-1.1.7/</guid>
      <description>

&lt;h2 id=&#34;section-1-1-7&#34;&gt;Section 1.1.7&lt;/h2&gt;

&lt;h3 id=&#34;exercise-1-6&#34;&gt;Exercise 1.6&lt;/h3&gt;

&lt;p&gt;If &lt;code&gt;if&lt;/code&gt; is not a special form, and is a procedure based on &lt;code&gt;cond&lt;/code&gt; instead, it
would lose its short-circuiting property, as, due to applicative-order
evaluation, all of the arguments would need be evaluated just as the
&lt;code&gt;new-if&lt;/code&gt; procedure would be called.&lt;/p&gt;

&lt;p&gt;In our case, the program would fall into an infinite loop, always trying to
evaluate the next &lt;code&gt;sqrt-iter&lt;/code&gt;, even if &lt;code&gt;good-enough?&lt;/code&gt; would return true.&lt;/p&gt;

&lt;h3 id=&#34;exercise-1-7&#34;&gt;Exercise 1.7&lt;/h3&gt;

&lt;p&gt;For very small numbers, &lt;code&gt;0.001&lt;/code&gt; will be of the same order of magnitude with the
solution. This means that the tolerance will not be adequate to reach a
good-enough solution. For example, calculating the square root of &lt;code&gt;0.0001&lt;/code&gt;
itself (which is &lt;code&gt;0.01&lt;/code&gt;), our algorithm will yield:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-scheme&#34;&gt;&amp;gt; (sqrt 0.0001)
0.0323084483304812
&lt;/code&gt;&lt;/pre&gt;

&lt;pre&gt;&lt;code class=&#34;language-scheme&#34;&gt;&amp;gt; (square 0.032)
0.001024
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;For very large numbers, the algorithm might never terminate because the machine
precision will not be able to represent small differences between large numbers.
This means that &lt;code&gt;good-enough?&lt;/code&gt; will never return &lt;code&gt;#t&lt;/code&gt;. The tipping point is
between \( 10^{12} \) (which returns) and \( 10^{13} \) (which runs
infinitely).&lt;/p&gt;

&lt;p&gt;What happens is that after a few iterations, the &lt;code&gt;guess&lt;/code&gt; will be
&lt;code&gt;3,162,277.6601683795&lt;/code&gt;. When running &lt;code&gt;good-enough?&lt;/code&gt;, the procedure will try to
square it, yielding &lt;code&gt;10,000,000,000,000.002&lt;/code&gt;, which isn&amp;rsquo;t good enough according
to the &lt;code&gt;0.001&lt;/code&gt; desired error. When trying to improve this, the procedure will
need the average of the guess and of the &lt;code&gt;x/y = 3,162,277.660168379&lt;/code&gt;. However,
this reaches the limit of the numbers that can be represented, and the average
is again &lt;code&gt;3,162,277.6601683795&lt;/code&gt; which means &lt;code&gt;guess&lt;/code&gt; will remain unchanged and
the program will enter an infinite loop.&lt;/p&gt;

&lt;p&gt;To improve, we can use the following listing which uses the suggested solution
of making sure that the guess actually improves between runs, again using the
value &lt;code&gt;0.001&lt;/code&gt; as a threshold - if the ratio between or old guess and the new
one hasn&amp;rsquo;t changed at least by that number, then we consider our calculations
finished.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-klipse-eval-scheme&#34;&gt;(define (improve guess x)
  (average guess (/ x guess)))

(define (average x y)
  (/ (+ x y) 2))

(define (guess-improved? guess old-guess)
  (&amp;gt; (abs (- 1 (/ guess old-guess))) 0.001))

(define (sqrt-iter guess old-guess x)
  (if (guess-improved? guess old-guess)
    (sqrt-iter (improve guess x)
               guess
               x)
    guess))

(define (sqrt x)
  (sqrt-iter 1.0 x x))

(sqrt 0.0001)
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;exercise-1-8&#34;&gt;Exercise 1.8&lt;/h3&gt;

&lt;p&gt;We just need to use a different &lt;code&gt;improve&lt;/code&gt; procedure:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-scheme&#34;&gt;  (define (improve guess x)
    (/ (+ (/ x (square guess))
       (* 2 guess))
     3))
&lt;/code&gt;&lt;/pre&gt;
</description>
    </item>
    
    <item>
      <title>SICP Solutions: Introduction and section 1.1.6</title>
      <link>https://greenonion.github.io/blog/sicp-solutions-introduction-and-section-1.1.6/</link>
      <pubDate>Thu, 15 Jun 2017 19:39:51 +0300</pubDate>
      
      <guid>https://greenonion.github.io/blog/sicp-solutions-introduction-and-section-1.1.6/</guid>
      <description>

&lt;h1 id=&#34;introduction&#34;&gt;Introduction&lt;/h1&gt;

&lt;p&gt;I have been studying &lt;a href=&#34;https://mitpress.mit.edu/sicp/&#34;&gt;Structure and Interpretation of Computer Programs&lt;/a&gt; for
about one and an half months now. As everyone familiar with the book will know,
at least half of the work (and the knowledge!) is in its several exercises. I
started documenting my progress, writing down all solutions, but wasn&amp;rsquo;t sure
whether I wanted to post them here. At first I wasn&amp;rsquo;t even sure I&amp;rsquo;d actually go
through with the book, but as I&amp;rsquo;m reaching section 2.2.4 I am amazed by it and
not intending to stop soon. Still, this doesn&amp;rsquo;t mean I&amp;rsquo;ll make it to the end,
and I&amp;rsquo;m not stressing about how long it will take. I&amp;rsquo;m here for the long haul.
It also goes without saying that you shouldn&amp;rsquo;t believe everyhting you read: some
of these solutions might be incomplete or just plain wrong. Also usually I
compare my notes to &lt;a href=&#34;http://eli.thegreenplace.net/tag/sicp&#34;&gt;Eli Bendersky&amp;rsquo;s&lt;/a&gt;, he has done an amazing job documenting
his progress and certainly inspired me.&lt;/p&gt;

&lt;p&gt;On a slightly practical note: I have been using &lt;a href=&#34;https://racket-lang.org&#34;&gt;Racket&lt;/a&gt; with its &lt;a href=&#34;compatibility layer&#34;&gt;SICP
collection&lt;/a&gt; for the exercises. I considered using &lt;a href=&#34;https://clojure.org&#34;&gt;Clojure&lt;/a&gt; (which I&amp;rsquo;m
already familiar with) but decided against it, mainly because I wanted to follow
the text as closely as I could. Scheme is a very syntactically simple language
and it is no coincidence the authors chose it to teach programming.&lt;/p&gt;

&lt;p&gt;Finally, I have used the awesome &lt;a href=&#34;https://github.com/viebel/klipse&#34;&gt;Klipse&lt;/a&gt; Clojurescript plugin to make all
Scheme code samples interactive. This means you can change any value and see the
changes reflected on the result space!&lt;/p&gt;

&lt;p&gt;There are various solutions for the SICP exercises on the web, but these are
mine.&lt;/p&gt;

&lt;h1 id=&#34;chapter-1&#34;&gt;Chapter 1&lt;/h1&gt;

&lt;h2 id=&#34;section-1-1-6&#34;&gt;Section 1.1.6&lt;/h2&gt;

&lt;h3 id=&#34;exercise-1-1&#34;&gt;Exercise 1.1&lt;/h3&gt;

&lt;ul&gt;
&lt;li&gt;10&lt;/li&gt;
&lt;li&gt;12&lt;/li&gt;
&lt;li&gt;8&lt;/li&gt;
&lt;li&gt;3&lt;/li&gt;
&lt;li&gt;6&lt;/li&gt;
&lt;li&gt;nil&lt;/li&gt;
&lt;li&gt;nil&lt;/li&gt;
&lt;li&gt;19&lt;/li&gt;
&lt;li&gt;#f&lt;/li&gt;
&lt;li&gt;4&lt;/li&gt;
&lt;li&gt;16&lt;/li&gt;
&lt;li&gt;6&lt;/li&gt;
&lt;li&gt;16&lt;/li&gt;
&lt;/ul&gt;

&lt;h3 id=&#34;exercise-1-2&#34;&gt;Exercise 1.2&lt;/h3&gt;

&lt;pre&gt;&lt;code class=&#34;language-klipse-eval-scheme&#34;&gt;  (/ (+ 5 4 (- 2
               (- 3
                  (+ 6
                     (/ 4 5)))))
     (* 3 (- 6 2) (- 2 7))
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;exercise-1-3&#34;&gt;Exercise 1.3&lt;/h3&gt;

&lt;pre&gt;&lt;code class=&#34;language-klipse-eval-scheme&#34;&gt;(define (greatest-sum-squares a b c)
  (if (&amp;gt; a b)
  (+ (* a a)
  (if (&amp;gt; b c) (* b b) (* c c)))
  (+ (* b b)
  (if (&amp;gt; a c) (* a a) (* c c)))))

(greatest-sum-squares 2 3 4)
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;exercise-1-4&#34;&gt;Exercise 1.4&lt;/h3&gt;

&lt;p&gt;When the &lt;code&gt;if&lt;/code&gt; statement is evaluated, the operatior is chosen based on whether
&lt;code&gt;b&lt;/code&gt; is positive or negative, and results into calculating &lt;code&gt;a + |b|&lt;/code&gt;.&lt;/p&gt;

&lt;h3 id=&#34;exercise-1-5&#34;&gt;Exercise 1.5&lt;/h3&gt;

&lt;p&gt;Using applicative-order evaluation the evaluation never terminates, because as
it is trying to evaluate the operands, the algorithm falls into an infinite
loop.&lt;/p&gt;

&lt;p&gt;Using normal-order evaluation, the algorithm does not try to evaluate &lt;code&gt;(p)&lt;/code&gt;
until it actually needs it. However, the computation terminates before that, as
the &lt;code&gt;if&lt;/code&gt; special form evaluates the first branch and returns &lt;code&gt;0&lt;/code&gt;.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Home</title>
      <link>https://greenonion.github.io/home/</link>
      <pubDate>Sat, 11 Feb 2017 20:45:36 +0200</pubDate>
      
      <guid>https://greenonion.github.io/home/</guid>
      <description>&lt;p&gt;Thoughts on programming, data, and the art of stressing over a huge pile of
unread books.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>about</title>
      <link>https://greenonion.github.io/about/</link>
      <pubDate>Fri, 10 Feb 2017 00:00:00 +0000</pubDate>
      
      <guid>https://greenonion.github.io/about/</guid>
      <description>&lt;p&gt;Thanks to Husker Du for the name :)&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Lazy Evaluation in Clojure</title>
      <link>https://greenonion.github.io/blog/lazy-evaluation-in-clojure/</link>
      <pubDate>Wed, 02 Dec 2015 19:44:17 +0200</pubDate>
      
      <guid>https://greenonion.github.io/blog/lazy-evaluation-in-clojure/</guid>
      <description>&lt;p&gt;A couple of weeks ago I had to debug a really mind-boggling (for me)
problem at work and it took me the better part of three days to figure
it out. A Clojure program I had created in order to crunch some data
threw a stack overflow exception provided the input was large enough.
When I say large enough, I mean around 150 megabytes in JSON format.
So not huge, but neither negligible.&lt;/p&gt;

&lt;p&gt;Let me describe the situation. After some processing, I had a large
vector of hash maps that looked like this:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-clj&#34;&gt;[{&amp;quot;ab&amp;quot; [1 0]} {&amp;quot;bc&amp;quot; [1 1]} {&amp;quot;de&amp;quot; [1 0]} {&amp;quot;ab&amp;quot; [1 1]}]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;So each hash map was essentially mapping a string to a vector which
contained two numbers. The catch is that there are duplicate strings
there and the task was to merge them into one single hash map that
would contain the sum of each number in the vectors. So the example
above would become:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-clj&#34;&gt;{&amp;quot;ab&amp;quot; [2 1] &amp;quot;bc&amp;quot; [1 1] &amp;quot;de&amp;quot; [1 0]}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;“Easy enough”, I thought and proceeded to write:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-clj&#34;&gt;(defn merge-hashes-naive [coll]
  (apply merge-with (fn [a b] (map + a b))))
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Experienced Clojure developers should already be cringing with my
naivety.&lt;/p&gt;

&lt;p&gt;However, the program processed all the input easily and it wasn’t until
I added the next function that things got bad. After merging all the
hash maps, I needed to drop some of them and perform calculations based
on some business rules. I decided to use a list comprehension:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-clj&#34;&gt;(def simple-list-comp [results]
  (for [[k [a b]] results]
    :when ;; do stuff
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;For small input, this worked fine. It wasn’t until I tried it using
the full input that the stack overflow exception was thrown. And the
stack trace was huge:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;java.lang.StackOverflowError
     at clojure.core$seq.invoke(core.clj:133)
     at clojure.core$concat$fn__3955.invoke(core.clj:685)
     at clojure.lang.LazySeq.sval(LazySeq.java:40)
     at clojure.lang.LazySeq.seq(LazySeq.java:49)
     at clojure.lang.RT.seq(RT.java:484)
     at clojure.core$seq.invoke(core.clj:133)
     ......
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The first thing I thought was “I saw this somewhere recently” and
quickly remembered Stuart Sierra’s &lt;a href=&#34;http://stuartsierra.com/2015/04/26/clojure-donts-concat&#34;&gt;post on the concat function&lt;/a&gt;
(I think his “&lt;a href=&#34;http://stuartsierra.com/tag/dos-and-donts&#34;&gt;Clojure do’s and dont’s&lt;/a&gt;”
series is mandatory reading for Clojure programmers). He writes:&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;This is a nasty bug in production code, because it could occur far
away from its source, and the accumulated stack frames of seq prevent
us from seeing where the error originated.&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;After trying a lot of things to solve it, all to no avail — the program
had much more going on than these two functions — I decided to try to
read the bottom of the (usually truncated) stack trace. Fortunately,
there is a JVM option for that:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;-XX:MaxJavaStackTraceDepth=-1
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Long story short, the stack trace pointed to the second line of the
&lt;code&gt;simple-list-comp&lt;/code&gt; function. I thought a bit about it, decided it was
random and ignored it completely.&lt;/p&gt;

&lt;p&gt;A couple of days passed.&lt;/p&gt;

&lt;p&gt;By then it was Friday, and this had taken me almost half a week to
solve. I could just use a larger stack, but knew that I would have
to deal with it eventually. So I thought I would take another look at
the &lt;code&gt;simple-list-function&lt;/code&gt;. I removed all logic from it and just let it
pass once through the input. Stack overflow. Then, I changed the
destructuring:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-clj&#34;&gt;(for [[a b] results] a)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;em&gt;It didn’t blow up.&lt;/em&gt; Somehow, the destructuring of the vector was where
the problem occurred (note to self: I should trust the stack traces
more). Suddenly, it all clicked: &lt;em&gt;The destructuring triggered the
evaluation, it was all lazy seqs until that point!&lt;/em&gt; But of course!
The map function wraps its result in a lazy seq, meaning that nothing
gets evaluated until the result is needed.&lt;/p&gt;

&lt;p&gt;What this means is that whenever the call to &lt;code&gt;merge-with&lt;/code&gt; encountered
a key it had seen before, instead of calculating the result, it wrapped
the calculation inside a lazy seq and returned. For example, merging:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-clj&#34;&gt;[{&amp;quot;ab&amp;quot; [1 0]} {&amp;quot;ab&amp;quot; [2 1]} {&amp;quot;ab&amp;quot; [0 1]}]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;instead of returning just:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-clj&#34;&gt;{&amp;quot;ab&amp;quot; [3 2]}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;would essentially return:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-clj&#34;&gt;{&amp;quot;ab&amp;quot; (map + [0 1] (map + [2 1] [1 0]))}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;where the nested maps would only get evaluated when needed, which in my
case was when destructuring the list comprehension arguments. Each
nested call would consume a bit of the stack and when the nesting
became large enough, the stack would overflow. In my case it took about
1,500 calls (so a key would exist 1,500 times in the original
collection).&lt;/p&gt;

&lt;p&gt;The easiest mitigation was to replace &lt;code&gt;map&lt;/code&gt; with its eager counterpart,
&lt;code&gt;mapv&lt;/code&gt;:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-clj&#34;&gt;(defn merge-hashes-naive [coll]
  (apply merge-with (fn [a b] (mapv + a b))))
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;But the real gain was finally understanding that lazy evaluation
happens all the time in Clojure. For example &lt;code&gt;map&lt;/code&gt;, &lt;code&gt;filter&lt;/code&gt;, &lt;code&gt;range&lt;/code&gt;
and &lt;code&gt;take&lt;/code&gt; are all lazy.&lt;/p&gt;

&lt;p&gt;I hope this will be helpful to another Clojure newbie in the future.
For more about lazy evaluation in Clojure I suggest reading &lt;a href=&#34;http://clojure-doc.org/articles/language/laziness.html&#34;&gt;Laziness in Clojure&lt;/a&gt;
and chapter 3 from the excellent &lt;a href=&#34;https://www.manning.com/books/the-joy-of-clojure-second-edition&#34;&gt;Joy of Clojure&lt;/a&gt;
book.&lt;/p&gt;
</description>
    </item>
    
  </channel>
</rss>